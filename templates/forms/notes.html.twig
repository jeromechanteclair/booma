
<div class="row" >
    <div class="col-md-12">
        <div class="col-md-12">{{ form_errors(form) }}</div>
        <div class="bg-danger">{{ form_errors(form.notes) }}</div>
        <div class="form-group input-group">
           
            
            {{ form_start(form) }}
             {{ form_widget(form.notes,  {'attr': {'placeholder':'Name', 'title':'Name','class':'form-control'}}) }}
              {{ form_row(form.save, { 'label': 'Submit me' }) }}
        {{ form_end(form, {'render_rest': false}) }}
        </div>
    </div>
    <div class="col-md-4">
        <div class="bg-danger"></div>
        <div class="form-group">
       
        </div>
    </div>
</div>
